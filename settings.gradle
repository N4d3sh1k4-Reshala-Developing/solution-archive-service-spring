rootProject.name = 'solution_archive-service'

def commonDir = file("../common")

if (commonDir.exists()) {
    includeBuild(commonDir) {
        dependencySubstitution {
            substitute module('com.n4d3sh1k4:common') using project(':')
        }
    }
}